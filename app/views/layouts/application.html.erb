<!DOCTYPE html>
<html>
  <head>
    <title>Bugs</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <style>
      body { color: white;}
      * {background-color: 	rgb(31, 31, 31); font-family: Arial !important;}
      button {color:white; background-color: gray}
      a {color: rgb(51, 153, 255); background-color:transparent; text-decoration: none;}
      a:hover {opacity: 80%; text-decoration:underline}
      input {background-color: white}
      textarea {background-color: white}
      p {color: rgb(136,136,136)}
      .report {background-color:rgb(21, 21, 21);
       border-radius: 7px;word-wrap: break-word;
       width: 50%; min-width: 100px; padding-left: 10px; 
       padding-right: 10px; text-align:left; max-width: 800px; 
       padding-bottom: 10px;padding-top: 5px;  margin:auto; color:lightgray; margin-bottom:40px;  border:1px solid rgb(125, 125, 125);}
       .reports {margin:auto; text-align: center}
       
       .h2title {background-color:rgb(21, 21, 21); margin-top:5px}
      
    </style>
    <p class="notice" style="color:lightgreen"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <div>
      <h2><%= link_to "View All Bug Reports", root_path %>
      <% if user_signed_in? %>
      <%= link_to "Create Bug Report", new_bug_report_path %>
      <% else %>
        <%= link_to "Log In", new_user_session_path %>
        <%= link_to "Sign Up", new_user_registration_path %>
      <% end %></h2>
      
      </div>
    <% if user_signed_in? %>
    <p> <%= link_to "Profile Settings", edit_user_registration_path %></p> 
    <p>Logged in as: <%= current_user.email %> </p>
      <%= button_to "Log out", destroy_user_session_path, method: :delete %>
    
    <% end %>
    <%= yield %>
  </body>
</html>
